// config for backend server of ToDo App
export const config: {
  serverAddress: string,
  port: number,
  basePath: string,
} = {
  serverAddress: "http://localhost:8000",
  port: 8000,
  basePath: "http://localhost",
};